# FastAPI 실무 프로젝트 구조

## 프로젝트 구조

```
fastapi_project/
├── app/
│   ├── __init__.py
│   ├── main.py              # ⭐ FastAPI 앱 진입점
│   ├── config.py            # 환경 설정
│   ├── database.py          # DB 연결 설정
│   ├── dependencies.py      # 의존성 주입
│   │
│   ├── models/              # 🗄️ SQLAlchemy 모델 (DB 테이블)
│   │   ├── __init__.py
│   │   ├── base.py
│   │   └── user.py
│   │
│   ├── schemas/             # 📋 Pydantic 스키마 (요청/응답 검증)
│   │   ├── __init__.py
│   │   └── user.py
│   │
│   ├── routers/             # 🛣️ API 라우터 (엔드포인트)
│   │   ├── __init__.py
│   │   ├── auth.py
│   │   └── users.py
│   │
│   ├── services/            # 💼 비즈니스 로직
│   │   ├── __init__.py
│   │   ├── auth_service.py
│   │   └── user_service.py
│   │
│   ├── repositories/        # 🗃️ DB 접근 계층
│   │   ├── __init__.py
│   │   └── user_repository.py
│   │
│   └── utils/               # 🔧 유틸리티
│       ├── __init__.py
│       └── security.py
│
├── tests/                   # 🧪 테스트
│   ├── __init__.py
│   ├── conftest.py
│   └── test_users.py
│
├── requirements.txt
├── Dockerfile
├── docker-compose.yml
└── .env.example
```

## 요청 흐름 (Request Flow)

```
클라이언트 요청
    ↓
┌─────────────────────────────────────────────────────────────┐
│  main.py (FastAPI App)                                       │
│    - 미들웨어 처리                                            │
│    - 라우터 등록                                              │
└─────────────────────────────────────────────────────────────┘
    ↓
┌─────────────────────────────────────────────────────────────┐
│  routers/*.py (라우터)                                       │
│    - URL 엔드포인트 정의                                      │
│    - 요청 데이터 검증 (Pydantic Schema)                       │
│    - 의존성 주입 (인증, DB 세션 등)                           │
└─────────────────────────────────────────────────────────────┘
    ↓
┌─────────────────────────────────────────────────────────────┐
│  services/*.py (서비스)                                      │
│    - 비즈니스 로직 처리                                       │
│    - 여러 Repository 조합                                     │
│    - 트랜잭션 관리                                            │
└─────────────────────────────────────────────────────────────┘
    ↓
┌─────────────────────────────────────────────────────────────┐
│  repositories/*.py (리포지토리)                              │
│    - DB CRUD 작업                                            │
│    - 쿼리 로직                                               │
└─────────────────────────────────────────────────────────────┘
    ↓
┌─────────────────────────────────────────────────────────────┐
│  models/*.py (모델)                                          │
│    - SQLAlchemy ORM 모델                                     │
│    - DB 테이블 정의                                          │
└─────────────────────────────────────────────────────────────┘
    ↓
  Database (PostgreSQL)
```

## 각 레이어 역할

### 1. Router (routers/)
- HTTP 엔드포인트 정의
- 요청/응답 처리
- **비즈니스 로직 없음** (Service에 위임)

### 2. Service (services/)
- 비즈니스 로직 담당
- 여러 Repository 조합
- 트랜잭션 관리

### 3. Repository (repositories/)
- DB 접근 전담
- CRUD 메서드 제공
- 쿼리 로직

### 4. Model (models/)
- DB 테이블 구조 정의
- SQLAlchemy ORM

### 5. Schema (schemas/)
- 요청/응답 데이터 검증
- Pydantic 모델

## 실행 방법

```bash
# 가상환경 생성
python -m venv venv
source venv/bin/activate  # Windows: venv\Scripts\activate

# 의존성 설치
pip install -r requirements.txt

# 서버 실행
uvicorn app.main:app --reload

# API 문서
# http://localhost:8000/docs (Swagger UI)
# http://localhost:8000/redoc (ReDoc)
```

## Docker 실행

```bash
docker-compose up -d
```
